$('#invoices').html('#{escape_javascript render(@invoices)}');
$('#paginator').html('#{escape_javascript(paginate(@invoices, :remote => true).to_s)}');
$('#entry_info').html('#{escape_javascript(page_entries_info(@invoices).to_s)}');
